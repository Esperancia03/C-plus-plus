#include<iostream>
#include <iomanip>

using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;
    float velocity = float(x)/float(y);
    cout << fixed << setprecision(6) << velocity << endl;
}
